{
    "_comment": "Per-module-GUID deny-list of configuration fields whose values must never be returned by get_instance. Keys are Symcon module GUIDs (with braces). The heuristic /(pass|secret|token|api[_-]?key|client[_-]?secret|credential|pin)/i is applied additionally and cannot be disabled.",
    "{0000-PLACEHOLDER-MODULE-GUID-0000}": [
        "ExampleApiKey",
        "ExamplePassword"
    ]
}
